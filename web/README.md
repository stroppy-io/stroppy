# Stroppy Cloud Panel - Frontend

Современное frontend приложение для управления облачными ресурсами, построенное на React, TypeScript, Vite, Tailwind CSS и Ant Design.

## Технологический стек

- **React 19** - библиотека для создания пользовательских интерфейсов
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый инструмент сборки и dev-сервер
- **Tailwind CSS** - utility-first CSS фреймворк
- **Ant Design 5.0** - библиотека React компонентов
- **Yarn** - менеджер пакетов

## Установка и запуск

### Установка зависимостей
```bash
yarn install
```

### Запуск в режиме разработки
```bash
yarn dev
```

### Сборка для продакшена
```bash
yarn build
```

### Предварительный просмотр сборки
```bash
yarn preview
```

### Проверка типов
```bash
yarn type-check
```

### Линтинг
```bash
yarn lint
```

### Очистка папки сборки
```bash
yarn clean
```

## Структура проекта

```
web/
├── public/                 # Статические файлы
├── src/
│   ├── assets/            # Ресурсы (изображения, иконки)
│   ├── components/        # React компоненты
│   ├── pages/            # Страницы приложения
│   ├── hooks/            # Пользовательские хуки
│   ├── utils/            # Утилиты и хелперы
│   ├── types/            # TypeScript типы
│   ├── App.tsx           # Главный компонент
│   ├── main.tsx          # Точка входа
│   └── index.css         # Глобальные стили (Tailwind)
├── tailwind.config.js    # Конфигурация Tailwind
├── postcss.config.js     # Конфигурация PostCSS
├── vite.config.ts        # Конфигурация Vite
├── tsconfig.json         # Конфигурация TypeScript
└── package.json          # Зависимости и скрипты
```

## Особенности

- 🚀 **Быстрая разработка** - Vite обеспечивает мгновенную перезагрузку
- 🎨 **Современный UI** - Ant Design компоненты с кастомизацией через Tailwind
- 📱 **Адаптивный дизайн** - мобильно-первый подход
- 🔧 **TypeScript** - полная типизация для надежности кода
- ⚡ **Оптимизация** - автоматическая оптимизация сборки

## Разработка

Приложение автоматически откроется в браузере по адресу `http://localhost:5173` при запуске `yarn dev`.

Все изменения в коде будут автоматически отражены в браузере благодаря Hot Module Replacement (HMR).
