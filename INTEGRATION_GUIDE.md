# Руководство по интеграции Frontend и Backend

## Обзор

Приложение теперь полностью интегрировано с авторизацией и защищенными маршрутами:

### ✅ Реализованные функции:

1. **Авторизация**:
   - Страница входа в систему (`/login`)
   - Страница регистрации (`/register`)
   - JWT токены для аутентификации
   - Автоматический редирект на login при отсутствии авторизации

2. **Защищенные маршруты**:
   - Все основные страницы приложения защищены авторизацией
   - Компонент `ProtectedRoute` для проверки аутентификации
   - Сохранение состояния авторизации в localStorage

3. **API интеграция**:
   - Полнофункциональный API клиент с axios
   - CRUD операции для запусков
   - Обработка ошибок и состояний загрузки
   - Автоматическое добавление JWT токена к запросам

4. **Пользовательский интерфейс**:
   - Информация о пользователе в боковой панели
   - Кнопка выхода из системы
   - Обновленные страницы с интеграцией API

## Запуск приложения

### 1. Запуск Backend

```bash
cd backend
make run
# или
./bin/stroppy-cloud-pannel
```

Backend запустится на `http://localhost:8080`

### 2. Запуск Frontend

```bash
cd frontend
npm run dev
```

Frontend запустится на `http://localhost:5173`

### 3. Настройка переменных окружения (опционально)

Создайте файл `.env` в папке `frontend` (скопируйте из `env.example`):

```bash
cd frontend
cp env.example .env
```

Измените URL API если необходимо:
```env
VITE_API_BASE_URL=http://localhost:8080/api/v1
```

## Тестирование интеграции

### 1. Тест авторизации

1. Откройте `http://localhost:5173`
2. Вы должны быть перенаправлены на `/login`
3. Перейдите на "Создать новый аккаунт"
4. Зарегистрируйтесь с любым именем пользователя (мин. 3 символа) и паролем (мин. 6 символов)
5. После успешной регистрации вы должны быть автоматически авторизованы и перенаправлены на главную страницу

### 2. Тест CRUD операций

1. После авторизации перейдите на страницу "Запуски"
2. Нажмите "Новый запуск" для создания запуска
3. Заполните форму и создайте запуск
4. Проверьте, что запуск появился в списке
5. Попробуйте удалить запуск через меню действий

### 3. Тест состояний

1. Проверьте состояние загрузки при обновлении списка запусков
2. Попробуйте выйти из системы через меню пользователя в боковой панели
3. Убедитесь, что после выхода вы перенаправлены на страницу входа
4. Проверьте, что токен сохраняется при перезагрузке страницы

## API Endpoints

Backend предоставляет следующие endpoints:

### Аутентификация
- `POST /api/v1/auth/register` - Регистрация
- `POST /api/v1/auth/login` - Вход в систему

### Пользователи (защищенные)
- `GET /api/v1/profile` - Профиль пользователя

### Запуски (защищенные)
- `GET /api/v1/runs` - Список запусков (с пагинацией)
- `POST /api/v1/runs` - Создание запуска
- `GET /api/v1/runs/:id` - Получение запуска по ID
- `PUT /api/v1/runs/:id` - Обновление запуска
- `PUT /api/v1/runs/:id/status` - Обновление статуса запуска
- `DELETE /api/v1/runs/:id` - Удаление запуска

### Служебные
- `GET /health` - Проверка состояния сервера

## Структура проекта

```
├── backend/
│   ├── cmd/stroppy-cloud-pannel/     # Главное приложение
│   ├── internal/                     # Внутренние пакеты
│   │   ├── domain/                   # Доменные модели
│   │   ├── repository/               # Репозитории
│   │   ├── service/                  # Бизнес-логика
│   │   ├── handler/                  # HTTP обработчики
│   │   └── middleware/               # Middleware
│   └── pkg/                          # Переиспользуемые пакеты
│
└── frontend/
    ├── src/
    │   ├── components/               # React компоненты
    │   ├── contexts/                 # React контексты
    │   ├── pages/                    # Страницы приложения
    │   ├── services/                 # API клиенты
    │   └── types/                    # TypeScript типы
    └── public/
```

## Возможные проблемы

### CORS ошибки
Backend настроен с CORS middleware, но если возникают проблемы:
1. Убедитесь, что backend запущен на правильном порту
2. Проверьте настройки CORS в `main.go`

### Проблемы с токенами
1. Проверьте, что JWT_SECRET установлен в backend
2. Очистите localStorage в браузере: `localStorage.clear()`
3. Перезапустите backend с новым секретом

### База данных
1. База данных SQLite создается автоматически при первом запуске
2. Файл базы данных: `backend/stroppy.db`
3. Для сброса данных удалите файл базы данных

## Следующие шаги

1. **Добавить валидацию форм** - улучшить UX с более детальной валидацией
2. **Реализовать профиль пользователя** - страница редактирования профиля
3. **Добавить уведомления** - toast уведомления для всех действий
4. **Оптимизировать производительность** - lazy loading, мемоизация
5. **Добавить тесты** - unit и integration тесты для frontend
6. **Улучшить UI/UX** - более современный дизайн и анимации

## Технологический стек

### Backend
- **Go 1.21+** - основной язык
- **Gin** - HTTP веб-фреймворк
- **SQLite** - база данных
- **JWT** - аутентификация
- **bcrypt** - хеширование паролей

### Frontend
- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **Ant Design** - UI компоненты
- **Axios** - HTTP клиент
- **React Router** - маршрутизация

Интеграция завершена и готова к использованию! 🎉
