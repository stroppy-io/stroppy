# Добавление runId во все компоненты

## 🎯 Выполненные изменения

### ✅ Добавлен runId во все интерфейсы и компоненты

RunId теперь присутствует везде - в типах данных, форме создания, таблице, модальных окнах и моковых данных.

## 🔧 Технические изменения

### **1. Обновлены интерфейсы типов**

**RunCreationFormData:**
```typescript
export interface RunCreationFormData {
  // Основная информация о запуске
  runId: string        // ← НОВОЕ ПОЛЕ
  name: string
  description?: string
  status: 'completed' | 'failed'
  // ...
}
```

**Run интерфейсы (RunsPage.tsx и RunComparisonModal.tsx):**
```typescript
interface Run {
  id: string
  runId: string        // ← НОВОЕ ПОЛЕ
  name: string
  description?: string
  status: 'completed' | 'failed'
  // ...
}
```

### **2. Обновлена форма создания запуска**

**Новая структура полей (6/6/6/6 layout):**
```typescript
<Row gutter={16}>
  <Col span={6}>
    <Form.Item name="runId" label="ID запуска" rules={[{ required: true }]}>
      <Input placeholder="run-001" />
    </Form.Item>
  </Col>
  <Col span={6}>
    <Form.Item name="name" label="Название запуска" rules={[{ required: true }]}>
      <Input placeholder="Введите название запуска" />
    </Form.Item>
  </Col>
  <Col span={6}>
    <Form.Item name="description" label="Описание">
      <Input placeholder="Краткое описание запуска" />
    </Form.Item>
  </Col>
  <Col span={6}>
    <Form.Item name="status" label="Статус запуска" rules={[{ required: true }]}>
      <Select>
        <Option value="completed">Завершен</Option>
        <Option value="failed">Ошибка</Option>
      </Select>
    </Form.Item>
  </Col>
</Row>
```

**Особенности:**
- **Обязательное поле** с валидацией
- **Placeholder**: "run-001" как пример формата
- **Первая позиция** в форме для важности
- **Включено в handleSubmit** для отправки данных

### **3. Обновлена таблица запусков**

**Новая колонка "ID и название":**
```typescript
{
  title: 'ID и название',
  dataIndex: 'name',
  key: 'name',
  width: 280, // Увеличена с 250px
  render: (text: string, record: Run) => (
    <div>
      <div className="text-xs text-gray-500 font-mono mb-1">{record.runId}</div>
      <div className="font-medium">{text}</div>
      {record.description && (
        <div className="text-sm text-gray-500 mt-1">{record.description}</div>
      )}
      <div className="flex items-center space-x-2 mt-2">
        <Tag color="blue">{record.workloadType.toUpperCase()}</Tag>
        <Tag color="green">{record.databaseType}</Tag>
      </div>
    </div>
  )
}
```

**Особенности отображения:**
- **runId**: Мелкий серый моноширинный шрифт сверху
- **Название**: Основной жирный текст
- **Описание**: Серый текст под названием
- **Теги**: Цветные теги внизу

### **4. Обновлено модальное окно деталей**

**runId в заголовке:**
```typescript
<div className="flex justify-between items-start">
  <div>
    <div className="text-sm text-gray-500 font-mono mb-2">{selectedRun.runId}</div>
    <Title level={3} className="mb-2">{selectedRun.name}</Title>
    {/* ... остальное содержимое */}
  </div>
</div>
```

**Особенности:**
- **Позиция**: Самый верх, над названием
- **Стиль**: Мелкий серый моноширинный шрифт
- **Отступ**: mb-2 от названия для визуального разделения

### **5. Обновлено модальное окно сравнения**

**runId в разделе "Основная информация":**
```typescript
<Card title={<><SettingOutlined className="mr-2" />Основная информация</>}>
  {renderComparisonField('Run ID', run1.runId, run2.runId, true)}
  {renderComparisonField('Описание', run1.description || 'Не указано', run2.description || 'Не указано')}
  // ... остальные поля
</Card>
```

**Особенности:**
- **Первое поле** в сравнении
- **Highlight**: `true` для подсветки различий
- **Трехколоночная структура**: заголовок + runId1 + runId2

### **6. Обновлены моковые данные**

**Реалистичные runId для тестовых данных:**
```typescript
const runs: Run[] = [
  {
    id: '1',
    runId: 'run-pgbench-001',    // ← PostgreSQL pgbench
    name: 'Тест производительности PostgreSQL',
    // ...
  },
  {
    id: '2', 
    runId: 'run-ycsb-002',       // ← Greenplum YCSB
    name: 'YCSB тест Greenplum',
    // ...
  },
  {
    id: '3',
    runId: 'run-tpch-003',       // ← ClickHouse TPC-H
    name: 'TPC-H тест ClickHouse',
    // ...
  },
  {
    id: '4',
    runId: 'run-tpcc-004',       // ← Cloudberry TPC-C
    name: 'TPC-C тест Cloudberry',
    // ...
  }
]
```

**Формат runId:**
- **Префикс**: `run-`
- **Тип теста**: `pgbench`, `ycsb`, `tpch`, `tpcc`
- **Номер**: `001`, `002`, `003`, `004`
- **Итоговый формат**: `run-{тест}-{номер}`

## 🎨 Визуальное отображение

### **В таблице:**
```
run-pgbench-001
Тест производительности PostgreSQL
Нагрузочное тестирование с pgbench
[PGBENCH] [postgres]
```

### **В модальном окне деталей:**
```
run-pgbench-001
Тест производительности PostgreSQL

Нагрузочное тестирование с pgbench
[Завершен] [PGBENCH] [postgres]
```

### **В сравнении:**
```
| Run ID | run-pgbench-001 | run-ycsb-002 |
```

## 📊 Места отображения runId

1. **✅ Форма создания** - поле ввода (обязательное)
2. **✅ Таблица запусков** - над названием (мелкий шрифт)
3. **✅ Модальное окно деталей** - в заголовке (над названием)
4. **✅ Модальное окно сравнения** - первое поле сравнения
5. **✅ Моковые данные** - реалистичные примеры

## ✅ Проверка качества

- ✅ **Сборка проекта** - успешна без ошибок
- ✅ **Проверка типов** - все типы корректны
- ✅ **Интерфейсы** - runId добавлен во все нужные места
- ✅ **Валидация** - обязательное поле в форме
- ✅ **Визуальное отображение** - консистентное во всех компонентах

## 🚀 Готовность

**runId полностью интегрирован во все компоненты системы:**
- **Форма создания** - пользователь может указать runId
- **Таблица** - runId отображается над названием каждого запуска
- **Детали** - runId показывается в заголовке модального окна
- **Сравнение** - runId участвует в сравнении запусков
- **Типы данных** - все интерфейсы обновлены

**Готово к использованию!** 🎉
