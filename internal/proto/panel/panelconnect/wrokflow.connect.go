// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: panel/wrokflow.proto

package panelconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	panel "github.com/stroppy-io/stroppy-cloud-panel/internal/proto/panel"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// WorkflowServiceName is the fully-qualified name of the WorkflowService service.
	WorkflowServiceName = "panel.WorkflowService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// WorkflowServiceGetWorkflowProcedure is the fully-qualified name of the WorkflowService's
	// GetWorkflow RPC.
	WorkflowServiceGetWorkflowProcedure = "/panel.WorkflowService/GetWorkflow"
	// WorkflowServiceCancelWorkflowProcedure is the fully-qualified name of the WorkflowService's
	// CancelWorkflow RPC.
	WorkflowServiceCancelWorkflowProcedure = "/panel.WorkflowService/CancelWorkflow"
)

// WorkflowServiceClient is a client for the panel.WorkflowService service.
type WorkflowServiceClient interface {
	GetWorkflow(context.Context, *panel.Ulid) (*panel.Workflow, error)
	CancelWorkflow(context.Context, *panel.Ulid) (*panel.Workflow, error)
}

// NewWorkflowServiceClient constructs a client for the panel.WorkflowService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewWorkflowServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) WorkflowServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	workflowServiceMethods := panel.File_panel_wrokflow_proto.Services().ByName("WorkflowService").Methods()
	return &workflowServiceClient{
		getWorkflow: connect.NewClient[panel.Ulid, panel.Workflow](
			httpClient,
			baseURL+WorkflowServiceGetWorkflowProcedure,
			connect.WithSchema(workflowServiceMethods.ByName("GetWorkflow")),
			connect.WithClientOptions(opts...),
		),
		cancelWorkflow: connect.NewClient[panel.Ulid, panel.Workflow](
			httpClient,
			baseURL+WorkflowServiceCancelWorkflowProcedure,
			connect.WithSchema(workflowServiceMethods.ByName("CancelWorkflow")),
			connect.WithClientOptions(opts...),
		),
	}
}

// workflowServiceClient implements WorkflowServiceClient.
type workflowServiceClient struct {
	getWorkflow    *connect.Client[panel.Ulid, panel.Workflow]
	cancelWorkflow *connect.Client[panel.Ulid, panel.Workflow]
}

// GetWorkflow calls panel.WorkflowService.GetWorkflow.
func (c *workflowServiceClient) GetWorkflow(ctx context.Context, req *panel.Ulid) (*panel.Workflow, error) {
	response, err := c.getWorkflow.CallUnary(ctx, connect.NewRequest(req))
	if response != nil {
		return response.Msg, err
	}
	return nil, err
}

// CancelWorkflow calls panel.WorkflowService.CancelWorkflow.
func (c *workflowServiceClient) CancelWorkflow(ctx context.Context, req *panel.Ulid) (*panel.Workflow, error) {
	response, err := c.cancelWorkflow.CallUnary(ctx, connect.NewRequest(req))
	if response != nil {
		return response.Msg, err
	}
	return nil, err
}

// WorkflowServiceHandler is an implementation of the panel.WorkflowService service.
type WorkflowServiceHandler interface {
	GetWorkflow(context.Context, *panel.Ulid) (*panel.Workflow, error)
	CancelWorkflow(context.Context, *panel.Ulid) (*panel.Workflow, error)
}

// NewWorkflowServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewWorkflowServiceHandler(svc WorkflowServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	workflowServiceMethods := panel.File_panel_wrokflow_proto.Services().ByName("WorkflowService").Methods()
	workflowServiceGetWorkflowHandler := connect.NewUnaryHandlerSimple(
		WorkflowServiceGetWorkflowProcedure,
		svc.GetWorkflow,
		connect.WithSchema(workflowServiceMethods.ByName("GetWorkflow")),
		connect.WithHandlerOptions(opts...),
	)
	workflowServiceCancelWorkflowHandler := connect.NewUnaryHandlerSimple(
		WorkflowServiceCancelWorkflowProcedure,
		svc.CancelWorkflow,
		connect.WithSchema(workflowServiceMethods.ByName("CancelWorkflow")),
		connect.WithHandlerOptions(opts...),
	)
	return "/panel.WorkflowService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case WorkflowServiceGetWorkflowProcedure:
			workflowServiceGetWorkflowHandler.ServeHTTP(w, r)
		case WorkflowServiceCancelWorkflowProcedure:
			workflowServiceCancelWorkflowHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedWorkflowServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedWorkflowServiceHandler struct{}

func (UnimplementedWorkflowServiceHandler) GetWorkflow(context.Context, *panel.Ulid) (*panel.Workflow, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("panel.WorkflowService.GetWorkflow is not implemented"))
}

func (UnimplementedWorkflowServiceHandler) CancelWorkflow(context.Context, *panel.Ulid) (*panel.Workflow, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("panel.WorkflowService.CancelWorkflow is not implemented"))
}
