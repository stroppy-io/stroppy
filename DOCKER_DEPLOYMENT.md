# 🐳 Docker Deployment Guide

## Полноценный контейнер с админкой

Создан полноценный Docker контейнер, который объединяет frontend и backend в одном образе. Backend сервит статические файлы админки напрямую.

### ✅ Что реализовано

#### 🏗️ Многоэтапная сборка Docker
- **Этап 1**: Сборка frontend (React + TypeScript + Vite) с Yarn
- **Этап 2**: Сборка backend (Golang + Gin + SQLite) 
- **Этап 3**: Финальный минимальный образ Alpine Linux

#### 🔧 Функциональность контейнера
- ✅ Backend API на порту 8080
- ✅ Frontend админка сервится как статика через backend
- ✅ SPA маршрутизация (все пути ведут к index.html)
- ✅ API endpoints доступны по `/api/v1/*`
- ✅ Healthcheck на `/health`
- ✅ Автоматическая инициализация SQLite базы данных
- ✅ Персистентность данных через Docker volumes
- ✅ Безопасность: непривилегированный пользователь

#### 📁 Структура проекта
```
├── Dockerfile                 # Многоэтапная сборка
├── docker-compose.yml         # Продакшн развертывание  
├── docker-compose.dev.yml     # Развертывание для разработки
├── Makefile                   # Команды управления
├── .dockerignore              # Исключения для Docker
└── env.production.example     # Пример переменных окружения
```

## 🚀 Быстрый запуск

### Вариант 1: Docker Compose (рекомендуется)

```bash
# Сборка и запуск
make run

# Или напрямую
docker-compose up -d
```

Приложение будет доступно на: **http://localhost:8080**

### Вариант 2: Docker напрямую

```bash
# Сборка образа
make build

# Запуск контейнера
docker run -d \
  --name stroppy-cloud-panel \
  -p 8080:8080 \
  -v stroppy_data:/app/data \
  -e JWT_SECRET=your-secure-secret-key \
  stroppy-cloud-panel:latest
```

## 🛠️ Команды Makefile

```bash
make build      # Сборка Docker образа
make run        # Запуск в продакшн режиме  
make dev        # Запуск в режиме разработки
make stop       # Остановка контейнеров
make clean      # Очистка Docker ресурсов
make logs       # Просмотр логов
make test-api   # Тестирование API
make backup     # Резервное копирование данных
make help       # Справка по командам
```

## ⚙️ Конфигурация

### Переменные окружения

```bash
# Обязательные
JWT_SECRET=your-very-secure-secret-key    # JWT секрет (ВАЖНО!)

# Опциональные  
DB_PATH=/app/data/stroppy.db              # Путь к базе данных
STATIC_DIR=/app/web                       # Директория статики
PORT=8080                                 # Порт приложения
GIN_MODE=release                          # Режим Gin (release/debug)
TZ=Europe/Moscow                          # Временная зона
```

### Создание .env файла

```bash
# Скопируйте пример
cp env.production.example .env

# Отредактируйте переменные
nano .env
```

## 🌐 Архитектура приложения

### Маршрутизация в контейнере
```
http://localhost:8080/
├── /api/v1/*          → Backend API
├── /health            → Healthcheck  
├── /assets/*          → Статические ресурсы (CSS, JS)
├── /vite.svg          → Favicon
└── /*                 → Frontend SPA (index.html)
```

### Структура контейнера
```
/app/
├── stroppy-cloud-pannel    # Backend бинарный файл
├── web/                    # Frontend статика
│   ├── index.html
│   └── assets/
├── data/                   # База данных (volume)
│   └── stroppy.db
```

## 🔒 Безопасность

- ✅ Непривилегированный пользователь `appuser`
- ✅ Минимальный Alpine Linux образ
- ✅ JWT токены для аутентификации
- ✅ bcrypt хеширование паролей
- ✅ Изолированная файловая система
- ✅ Healthcheck для мониторинга

## 📊 Мониторинг

### Healthcheck
```bash
curl http://localhost:8080/health
# Ответ: {"status":"ok","timestamp":1640995200}
```

### Логи
```bash
# Просмотр логов
make logs

# Или напрямую
docker-compose logs -f
```

### Статистика ресурсов
```bash
make monitor

# Или напрямую  
docker stats stroppy-cloud-panel
```

## 🔄 Backup и восстановление

### Создание резервной копии
```bash
make backup
# Создаст файл: stroppy_backup_YYYYMMDD_HHMMSS.tar.gz
```

### Восстановление данных
```bash
# Остановите контейнер
make stop

# Восстановите данные
docker run --rm \
  -v stroppy-cloud-panel_stroppy_data:/data \
  -v $(PWD):/backup \
  alpine tar xzf /backup/your_backup_file.tar.gz -C /data

# Запустите контейнер
make run
```

## 🚧 Разработка

### Режим разработки
```bash
# Запуск с hot-reload
make dev

# Backend: http://localhost:8080  
# Frontend: http://localhost:5173
```

### Отладка
```bash
# Вход в контейнер
make shell

# Или напрямую
docker exec -it stroppy-cloud-panel /bin/sh
```

## 🐛 Troubleshooting

### Проблемы с портами
```bash
# Проверка занятых портов
netstat -tlnp | grep 8080

# Изменение порта
docker-compose up -d -e PORT=8081
```

### Проблемы с базой данных
```bash
# Сброс базы данных
make stop
docker volume rm stroppy-cloud-panel_stroppy_data
make run
```

### Проблемы с правами
```bash
# Проверка владельца файлов в контейнере
docker exec stroppy-cloud-panel ls -la /app/data/
```

## 📈 Производительность

### Оптимизация образа
- ✅ Многоэтапная сборка (уменьшает размер)
- ✅ Alpine Linux (минимальный размер)
- ✅ Статическая компиляция Go
- ✅ Оптимизированная сборка frontend

### Размер образа
```bash
docker images stroppy-cloud-panel:latest
# Ожидаемый размер: ~50-100MB
```

## 🔄 CI/CD Integration

### GitHub Actions пример
```yaml
name: Build and Deploy
on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker image
        run: make build
      - name: Test API
        run: make test-api
      - name: Deploy
        run: make run
```

## 📋 Checklist перед продакшеном

- [ ] Изменен JWT_SECRET на безопасный ключ
- [ ] Настроены переменные окружения
- [ ] Настроены Docker volumes для данных
- [ ] Настроен мониторинг и логирование
- [ ] Настроены резервные копии
- [ ] Проведено нагрузочное тестирование
- [ ] Настроен reverse proxy (nginx) при необходимости

---

**🎉 Контейнер готов к продакшену!**

Полноценное приложение с админкой в одном контейнере:
- Backend API + Frontend SPA
- Автоматическая инициализация базы данных  
- Готовые конфигурации для разработки и продакшена
- Инструменты для мониторинга и резервного копирования
